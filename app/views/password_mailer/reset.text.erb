Hi, <%= params[:user].email %>,

You just requested a reset of your password.
Click the link to reset your password. the link will be expire automatically in 15 minutes.

<%= password_reset_edit_url(token: @token) %>
